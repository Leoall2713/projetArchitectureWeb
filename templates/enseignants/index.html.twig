{% extends 'base.html.twig' %}

{% block title %}Enseignants index{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Enseigants</h1>

        <div class="table-responsive">

    <table class="table table-striped table-bordered">
            <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Nom_enseigant</th>
                <th>Prenom_enseignant</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enseignant in enseignants %}
            <tr>
                <td>{{ enseignant.id }}</td>
                <td>{{ enseignant.nomEnseigant }}</td>
                <td>{{ enseignant.prenomEnseignant }}</td>
                <td>{{ enseignant.email }}</td>
                <td>
                    <a href="{{ path('app_enseignants_show', {'id': enseignant.id}) }}"class="btn btn-info btn-sm">show</a>
                    <a href="{{ path('app_enseignants_edit', {'id': enseignant.id}) }}"class="btn btn-info btn-sm">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5" class="text-center">Aucun enseignant trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="mt-3">
    <a href="{{ path('app_enseignants_new') }}"class="btn btn-success">Ajouter un enseignant</a>
    </div>
</div>
{% endblock %}
